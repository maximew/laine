<div class="jumbotron text-center">
	<h1 class="text-center">Portes d'entrée</h1>

	<%= render 'admin/links' %>

	<hr>

	<%= link_to 'Nouvelle porte', new_admin_door_path, class: 'btn btn-lg btn-success center-block' %>

	<% @doors.each do |door| %>
		<div class="media">
		  <div class="media-left">
		      <%= image_tag door.image.url(:small) %>
		  </div>
		  <div class="media-body">
		  	<h2><%= door.category.libelle %></h2>
		  	<table class="table">
			  	<tr>
		        	<td><%= link_to 'Modifier', edit_admin_door_path(door), class: 'btn btn-default' %></td>
		        	<td><%= link_to 'Supprimer', admin_door_path(door), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
	        	</tr>
		  	</table>
		    
			
		  </div>
		</div>
	<% end %>
</div>